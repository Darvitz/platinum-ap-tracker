name: Validate JSON

on:
  pull_request:

jobs:
  validate-json:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Validate JSON files
        uses: OrRosenblatt/validate-json-action@1.1.0
        with:
          json: '**/*.json'